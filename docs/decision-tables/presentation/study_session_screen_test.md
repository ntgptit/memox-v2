# Decision Tables: study_session_screen_test

Test file: `test/presentation/study_session_screen_test.dart`

## Decision table: onOpen

| ID | Branch / condition | Given | When | Then | Coverage |
| --- | --- | --- | --- | --- | --- |
| DT1 | `studySessionStateProvider(sessionId)` is unresolved and session screen enters loading branch | study session route opens with a pending `Completer<StudySessionSnapshot>` | the first frame is pumped before the future completes | `MxLoadingState` is rendered exactly once | C0+C1 |

## Decision table: onDisplay

| ID | Branch / condition | Given | When | Then | Coverage |
| --- | --- | --- | --- | --- | --- |
| DT1 | session is in progress and has a current guess-mode item | snapshot has `SessionStatus.inProgress`, one current item, `StudyMode.guess`, progress 40%, five session flashcards, and shuffled answers disabled | session screen renders active Guess branch | title `Guess`, toolbar actions, progress `40%`, target prompt `front 1` uses the smaller Guess prompt typography, five answer option cards are visible, and no old `Correct`, `Incorrect`, `Continue`, or `Skip card` controls are visible | C0+C1 |
| DT2 | session is terminal and has no current item | snapshot has `SessionStatus.completed`, `currentItem=null`, and no session flashcards | session screen renders terminal branch | `This session has ended.` and `View result` are visible | C0+C1 |
| DT3 | session is in progress and has a current review-mode item | snapshot has `StudyMode.review`, one current item, and one review flashcard | session screen renders active review branch | title `Review`, text settings/audio/more actions, edit/audio card actions, and both card faces are visible | C0+C1 |
| DT4 | review-mode local page index starts at the first card and progress row uses larger synchronized sizing | snapshot has `StudyMode.review` and one review flashcard | review branch renders the progress row | progress label shows `0%`, the linear progress track uses the large height, and the percent label uses the larger review progress text role | C0+C1 |
| DT5 | review-mode branch replaces the old grading panel | snapshot has `StudyMode.review` and one review flashcard | review branch renders | `incorrect`, `correct`, and `Skip card` are not visible | C0+C1 |
| DT6 | review faces use fixed larger non-heavy typography | one review snapshot has a short front/back pair and another has a long back text | review branch renders the first card and then moves to the second card | front text is larger with medium weight, and both short and long answer texts keep the same regular-weight visible style | C0+C1 |
| DT7 | session is in progress and current mode is Match | snapshot has `StudyMode.match`, two current-round items, global progress 20%, and shuffle answers disabled | session screen renders active Match branch | title `Match`, toolbar actions, progress `20%`, and a two-column board with terms on the left and meanings on the right are visible | C0+C1 |
| DT8 | Match meaning tiles contain long definition text | snapshot has one long `back` value and two short `front` values in current round | Match branch renders the board | the long meaning is on the right, meaning tiles share the same fixed text style with `TextOverflow.ellipsis`, and no old `Skip card` control is shown | C0+C1 |
| DT9 | Match current round has more than one display batch | snapshot has seven current-round Match items and shuffle answers disabled | Match branch renders the first board | only the first five pairs are visible; pairs six and seven are hidden until the current display batch is completed | C0+C1 |
| DT10 | Match current display batch has only a few pairs | snapshot has two current-round Match items and the board has room for a five-pair layout | Match branch renders the board | each tile height matches a five-pair board slot instead of stretching to fill the remaining board height | C0+C1 |
| DT11 | Guess option list contains long meaning text | snapshot has `StudyMode.guess`, five answer options, and one distractor option with a long `back` value | Guess branch renders the option list | all meaning cards have equal height, the final card reaches the bottom of the options area, and long meaning text uses ellipsis instead of changing card height | C0+C1 |
| DT12 | session is in progress and has a current recall-mode item with hidden timed answer | snapshot has `StudyMode.recall`, progress 60%, one current item, and a long `back` value | session screen renders active Recall branch | title `Recall`, toolbar actions, large progress `60%`, two equal-height cards, hidden answer blur, timer action `Show (20s)`, and no old `Correct`, `Incorrect`, `Continue`, or `Skip card` controls are visible | C0+C1 |
| DT13 | session is in progress and has a current fill-mode item | snapshot has `StudyMode.fill`, progress 80%, one current item with term `front 1` and meaning `back 1` | session screen renders active Fill branch | title `Fill`, toolbar actions, progress `80%`, prompt card shows `back 1`, borderless answer input expands across the input card, `Help` / `Check` actions are visible, `Check` starts disabled, and old `Continue` / `Skip card` controls are hidden | C0+C1 |
| DT14 | Fill screen is focused while viewport height is reduced by keyboard insets | snapshot has `StudyMode.fill` and the test surface is short with a bottom view inset | Fill branch renders with the answer field focused | `resizeToAvoidBottomInset` keeps progress, prompt, input, and bottom actions in the widget tree without Flutter overflow exceptions | C0+C1 |

## Decision table: onUpdate

| ID | Branch / condition | Given | When | Then | Coverage |
| --- | --- | --- | --- | --- | --- |
| DT1 | single-card review is already on the last page and auto-submit timer reaches two seconds | review snapshot has exactly one card and repo fake counts batch submits | the first review page remains mounted for two seconds | repo receives one batch submit with `AttemptGrade.correct` | C0+C1 |
| DT2 | web mouse right-to-left drag advances review vocabulary and only the last card can auto-submit | review snapshot has two cards and repo fake counts batch submits | first page waits two seconds, then mouse drag moves from right to left to the last page and waits two more seconds | the visible vocabulary changes from card 1 to card 2; no batch submit happens on the first page; exactly one batch submit happens after the last-page delay | C0+C1 |
| DT3 | web mouse wheel scroll advances review vocabulary | review snapshot has two cards and repo fake counts batch submits | pointer scroll event fires over the review page with positive wheel delta | the visible vocabulary changes from card 1 to card 2 and no batch submit happens before the two-second last-page delay | C0+C1 |
| DT4 | Match correct selection completes one visible pair without database submit | Match snapshot has two pairs and fake repo counts match batch submits | user taps the matching left/right pair for card 1 | global progress changes from `20%` to `30%`, the pair keeps an opaque success highlight before fading/scaling out while preserving board slots, and fake repo has zero batch submits | C0+C1 |
| DT5 | Match mismatch stays local and resets tile selection | Match snapshot has two pairs and fake repo counts match batch submits | user taps a left tile and the wrong right tile | both tiles enter error feedback then return to idle, global progress remains `20%`, and fake repo has zero batch submits | C0+C1 |
| DT6 | Match full board submit writes mixed grades once | Match snapshot has two pairs and fake repo records item grade map | user mismatches card 1 once, then completes both pairs correctly | card 1 no longer increases progress after it is later matched because it remains staged as `incorrect`, and fake repo receives exactly one match batch with card 1 item `incorrect` and card 2 item `correct` | C0+C1 |
| DT7 | Match display batch completes before the whole round completes | Match snapshot has seven pairs and fake repo records match batch submits | user completes the first five pairs, waits for the transition, then completes pairs six and seven | global progress reaches `34%`, pairs six and seven appear after the first display batch, no DB submit happens before the final batch is complete, and the final submit contains all seven item grades | C0+C1 |
| DT8 | Guess correct option is selected on a single-item mode round | Guess snapshot has one current card, four distractor options, and fake repo counts mode item batches | user taps the option whose id matches the current flashcard id | selected option enters success feedback immediately, no DB submit happens before the feedback delay, then fake repo receives one mode batch with the current item mapped to `correct` | C0+C1 |
| DT9 | Guess incorrect option is selected on a single-item mode round | Guess snapshot has one current card, four distractor options, and fake repo counts mode item batches | user taps an option whose id differs from the current flashcard id | selected option enters error feedback, the correct option enters success feedback, progress remains unchanged, and fake repo receives one mode batch with the current item mapped to `incorrect` after the feedback delay | C0+C1 |
| DT10 | Guess option is tapped again while feedback is resolving | Guess snapshot has one current card, four distractor options, and fake repo counts mode item batches | user taps one option and then taps another before the feedback delay completes | the second tap is ignored and fake repo receives exactly one mode batch using the first selected option's grade | C0+C1 |
| DT11 | Recall answer is hidden and user requests reveal before timeout | Recall snapshot has a blurred answer with a running 20-second timer and fake repo counts mode item batches | user taps `Show (20s)` before timeout and waits for the reveal transition | answer switches to revealed scrollable content, `incorrect` and `correct` actions replace the timer, answer text has no hard `maxLines`, and fake repo has zero mode item batches | C0+C1 |
| DT12 | Recall revealed answer is reported incorrect on a single-item mode round | Recall snapshot is revealed and fake repo records mode item batches | user taps `incorrect` | progress remains unchanged and fake repo receives exactly one mode batch with the current item mapped to `incorrect` | C0+C1 |
| DT13 | Recall revealed answer is reported correct on a single-item mode round | Recall snapshot is revealed and fake repo records mode item batches | user taps `correct` | fake repo receives exactly one mode batch with the current item mapped to `correct` | C0+C1 |
| DT14 | Recall current item changes after answer/reload | mutable Recall snapshot starts at item 1 after answer reveal | provider emits item 2 for the same screen | prompt changes to item 2, answer returns to hidden blur, and `Show (20s)` replaces `incorrect` / `correct` | C0+C1 |
| DT15 | Recall submit is already in flight | Recall answer is revealed and fake repo keeps the first mode batch pending | user taps `correct` and then taps `incorrect` before the first submit completes | fake repo receives exactly one mode batch using the first selected `correct` grade | C0+C1 |
| DT16 | Recall hidden answer timer reaches timeout | Recall snapshot is hidden and fake repo counts mode item batches | 20 seconds elapse without tapping the timer action | answer auto-reveals, `Next` appears right-aligned, `incorrect` / `correct` are absent, and fake repo still has zero mode item batches | C0+C1 |
| DT17 | Recall timeout is advanced with next action on a single-item mode round | Recall snapshot is in timeout state and fake repo records mode item batches | user taps `Next` | progress remains unchanged and fake repo receives exactly one mode batch with the current item mapped to `incorrect` | C0+C1 |
| DT18 | Fill answer field is empty | Fill snapshot is in input state and fake repo counts mode item batches | user enters whitespace only | `Check` remains disabled and fake repo receives zero mode item batches | C0+C1 |
| DT19 | Fill entered term matches the prompt answer on a single-item mode round | Fill snapshot expects `front 1` and fake repo records mode item batches | user types ` Front 1 ` and taps `Check` | fake repo receives exactly one mode batch with the current item mapped to `correct` using trim/case-insensitive comparison | C0+C1 |
| DT20 | Fill entered term does not match the prompt answer | Fill snapshot expects `front 1` and fake repo counts mode item batches | user types `wrong` and taps `Check` | result state shows `wrong` plus `front 1`, progress remains unchanged, and only `Next` is visible; `Correct`, `Try again`, and refresh are absent; fake repo still has zero mode item batches | C0+C1 |
| DT21 | Fill result is advanced as incorrect on a single-item mode round | Fill snapshot is in result state after a wrong answer | user taps `Next` | progress remains unchanged and fake repo receives exactly one mode batch with the current item mapped to `incorrect` | C0+C1 |
| DT25 | Fill help is requested before typing | Fill snapshot is in input state and fake repo counts mode item batches | user taps `Help`, then taps `Next` from the help result state | help reveals the answer with only `Next`, no `Correct` action is shown, progress remains unchanged, no DB submit happens before `Next`, then fake repo receives one mode batch with the current item mapped to `incorrect` | C0+C1 |
| DT26 | Fill current item changes after local result state | mutable Fill snapshot starts at item 1 in result state | provider emits item 2 for the same screen | prompt changes to item 2, input/result state resets, answer field is empty, and result actions are no longer visible | C0+C1 |
| DT27 | Guess mode round contains multiple pending items | Guess snapshot has two current-round items and fake repo counts mode item batches | user answers the first item correctly, then answers the second item incorrectly | first answer advances to item 2 with zero DB submits; final answer submits one mode batch containing both staged grades | C0+C1 |
| DT28 | Recall mode round contains multiple pending items | Recall snapshot has two current-round items and fake repo counts mode item batches | user stages `correct` for item 1 and `incorrect` for item 2 | first answer advances to item 2 with zero DB submits; final answer submits one mode batch containing both staged grades | C0+C1 |
| DT29 | Fill mode round contains multiple pending items | Fill snapshot has two current-round items and fake repo counts mode item batches | user answers item 1 correctly, then answers item 2 incorrectly and taps `Next` | first answer advances to item 2 with zero DB submits; final action submits one mode batch containing both staged grades | C0+C1 |
| DT30 | Study speak buttons are front-side only | Guess snapshot renders a front prompt and back option with default speech settings | prompt speak button and option area are inspected | prompt button carries text `front 1` with side `front`, no back/meaning speak button is rendered, and no grade or progress mutation is required | C0+C1 |
