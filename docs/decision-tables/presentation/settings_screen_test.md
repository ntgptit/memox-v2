# Decision Tables: settings_screen_test

Test file: `test/presentation/settings_screen_test.dart`

## Decision table: onOpen

| ID | Branch / condition | Given | When | Then | Coverage |
| --- | --- | --- | --- | --- | --- |
| DT1 | settings overview opens with no linked account and default local settings | fresh `ProviderContainer` has no cloud account, default theme/locale providers, default study settings, and collapsed speech voice options | `/settings` settles and the user scrolls from Account toward Learning | one app-bar `Settings` title, signed-out Account summary, Personalization rows, current `System` values, Learning overview, and no eager TTS voice lookup are visible | C0+C1 |
| DT2 | learning detail waits for the study settings store | `studySettingsStoreProvider` is held by a pending future while the learning detail screen is open | the first frame of `LearningSettingsScreen` is pumped | `Learning experience`, `Loading study defaults`, and one shared loading state are visible | C0+C1 |
| DT3 | audio detail waits for the TTS settings repository | `ttsSettingsRepositoryProvider` is held by a pending future while the audio detail screen is open | the first frame of `AudioSpeechSettingsScreen` is pumped | `Audio & Speech`, `Loading speech settings`, and one shared loading state are visible | C0+C1 |

## Decision table: onDisplay

| ID | Branch / condition | Given | When | Then | Coverage |
| --- | --- | --- | --- | --- | --- |
| DT1 | overview islands use tokenized setting rows and status affordances | settings overview renders at normal width and text scale | page content is displayed and scrolled through the groups | Account and Audio overview entries expose tap/focus on the filled `MxCard` itself, Account content keeps a token-based focused minimum height, overview rows use `MxIconTile`, Personalization uses an edge-to-edge filled island with full-width flat `MxTappable` rows and `MxBadge` value chips instead of nested `MxListTile` focus pills, and shared island dividers remain token-based | C0+C1 |
| DT2 | audio settings are summarized on overview | TTS settings are loaded with auto-play off and no custom voice | overview scrolls to Audio & Speech | Audio overview shows `Text-to-Speech`, `Off · System voice`, and an `MxBadge` state chip, while voice selection and advanced voice controls are absent | C0+C1 |
| DT3 | study defaults are summarized on overview | settings store has New Study batch `10`, Review batch `20`, and shared study toggles enabled | overview scrolls to Learning experience | Learning overview shows `Study defaults` and `New 10 cards · Review 20 cards`, while detail-only batch row controls and study toggles are absent | C0+C1 |
| DT4 | persisted study batch defaults are outside the current valid ranges | SharedPreferences contains New Study batch `100` and Review batch `1` from an older app version | settings overview loads study defaults | provider state, settings store reads, and overview summary clamp to New Study `20` and Review `5` | C0+C1 |
| DT5 | OAuth config is missing on a non-Android platform | account provider has no Google client IDs and the platform is Windows | settings overview loads Account | missing-config guidance is visible and the overview does not expose a `Sign in with Google` CTA | C0+C1 |
| DT6 | linked account has Drive appdata scope | account store contains a Google account with `drive.appdata` granted | settings overview loads Account | account summary row shows avatar, identity, email, Drive-ready status, and chevron; sign-out is not exposed on overview | C0+C1 |
| DT7 | linked account is missing Drive appdata scope | account store contains a Google account without Drive appdata authorization | settings overview loads Account | account summary row shows identity with Drive reconnect-required status, while reconnect and sign-out actions are absent | C0+C1 |
| DT8 | web runtime account exists but Drive scope is missing | account store contains a Google account without Drive appdata authorization, auth restore returns a missing-scope account, and auth service requires the platform sign-in button | settings overview loads Account | account summary remains linked with reconnect-required status, while reconnect and sign-out actions are reserved for Account detail | C0+C1 |
| DT11 | compact overview density fits the top groups and removes generic ready copy | settings overview renders on a 390 by 844 phone viewport with a Drive-ready account and sync repository ready | content is displayed without scrolling | Account identity and email remain visible, the generic Drive-ready overview phrase is hidden, Account, Personalization, Appearance, and Language fit inside the initial viewport, and no standalone Drive Sync section competes for space | C0+C1 |
| DT12 | web stored Drive-ready account has no runtime Google session | account store contains a Drive-ready Google account, auth restore returns `signedOut`, auth service requires the platform button, and Drive Sync requires authorization | settings overview loads Account | account summary shows reconnect-required instead of Drive-ready and does not expose sync from overview | C0+C1 |
| DT13 | Account detail owns Drive Sync diagnostics | sync repository reports `failure` with a Google Drive API configuration message | `AccountSettingsScreen` settles | Account detail shows Drive Sync failure copy, technical diagnostic, and compact sync retry action | C0+C1 |
| DT14 | Account detail Drive Sync provider throws before returning status | sync repository throws `StateError` while Account detail loads Drive Sync | `AccountSettingsScreen` settles | Account detail renders one Drive Sync failure surface with diagnostic text, hides duplicate generic fallback copy, and keeps retry action available | C0+C1 |
| DT15 | Account detail Drive Sync has local sync metadata | sync repository reports `synced` with `lastSyncedAt` metadata | `AccountSettingsScreen` settles | `Google Drive is up to date.` and a short `Last synced:` helper line are visible inside Account detail | C0+C1 |
| DT16 | overview should not expose detail-only actions | overview has a Drive-ready account, Drive Sync can run, and study/TTS settings are loaded | user scrolls through the overview | sign-out, sync, standalone Drive Sync section, learning batch controls, TTS toggle, preview, and voice options are absent from the main Settings screen | C0+C1 |

## Decision table: onNavigate

| ID | Branch / condition | Given | When | Then | Coverage |
| --- | --- | --- | --- | --- | --- |
| DT1 | account summary is selected | settings overview is rendered inside a router with the Account child route registered | user taps the Account overview row | `AccountSettingsScreen` is rendered | C0+C1 |
| DT2 | account detail is the Drive Sync entrypoint | settings overview is rendered inside a router and Drive Sync can run | user taps the Account overview row | `AccountSettingsScreen` renders the Drive Sync section and compact sync action | C0+C1 |
| DT3 | Learning summary is selected | settings overview is rendered inside a router with the Learning child route registered | user scrolls to and taps the Learning overview row | `LearningSettingsScreen` is rendered | C0+C1 |
| DT4 | Audio summary is selected | settings overview is rendered inside a router with the Audio child route registered | user scrolls to and taps the Audio overview row | `AudioSpeechSettingsScreen` is rendered | C0+C1 |

## Decision table: onUpdate

| ID | Branch / condition | Given | When | Then | Coverage |
| --- | --- | --- | --- | --- | --- |
| DT1 | personalization rows update the selected theme and locale providers | settings overview is rendered with Personalization inline | user opens Appearance, taps `Dark`, opens Language, then taps `Vietnamese` | `themeModeProvider` becomes `ThemeMode.dark`, success snackbar appears, and `localeProvider` becomes `Locale('vi')` | C0+C1 |
| DT2 | compact width or large text scale still updates providers through sheets | settings overview is constrained to 320 by 640 with text scale 1.4 | user opens Appearance, taps `Dark`, opens Language, and taps `Vietnamese` | bottom-sheet option lists remain usable, `themeModeProvider` becomes dark, and `localeProvider` becomes Vietnamese | C0+C1 |
| DT3 | front-only speech controls update persisted settings and preview uses selected language | audio detail is rendered with fake TTS service | user enables `Text-to-Speech`, opens `Voice selection`, sets front language English, rate `0.7`, pitch `1.3`, volume `0.6`, then taps the compact preview icon in the sheet | `ttsSettingsProvider` stores auto-play, front language, rate, pitch, and volume, and fake service receives one English speak call with those audio values | C0+C1 |
| DT4 | front voice picker is a progressive disclosure section with friendly source and gender metadata | audio detail renders voice options collapsed and fake platform voices include local and network Korean engine IDs from the same voice family | user opens `Voice selection` and taps the compact voice options icon in the sheet | compact action tooltip changes to `Hide voice options`, front voice control shows memorable labels such as `Korean voice 1 · Device · Male` instead of raw engine IDs, the paired network option keeps the same voice number with `Online`, back voice stays hidden, and fake TTS service receives one Korean voice lookup request | C0+C1 |
| DT5 | study default controls save batch sizes and shared toggles | learning detail is rendered with default study settings and the redesigned summary metrics | user opens New Study batch, increases it, opens Review batch, decreases it, and disables shuffle flashcards, shuffle answers, and prioritize overdue | learning detail exposes `MxIconTile` and `MxBadge` summary affordances, provider state and SharedPreferences persist New Study batch `11`, Review batch `19`, and all three shared study toggles as disabled | C0+C1 |
| DT6 | Google sign-in succeeds and grants Drive appdata scope | account detail uses configured OAuth and fake Google auth returning a Drive-ready account | user taps `Sign in with Google` | account store persists subject/email/scope metadata and Account detail shows `Google Drive ready` | C0+C1 |
| DT7 | Google sign-in is canceled | account detail uses configured OAuth and fake Google auth returning canceled | user taps `Sign in with Google` | account remains signed out and no account metadata is stored | C0+C1 |
| DT8 | Google Drive scope is denied after sign-in | account detail fake Google auth returns account identity but no Drive appdata authorization | user taps `Sign in with Google` | account metadata is stored, Account detail shows `Google Drive reconnect required`, and no token is stored | C0+C1 |
| DT9 | user signs out locally | account detail has a Drive-ready Google account and study data exists in SharedPreferences | user taps the compact `Sign out` icon action | account store is cleared while study default settings remain unchanged | C0+C1 |
| DT10 | Google-rendered web reconnect restores runtime auth | settings overview starts with a stored Drive-ready account but missing Web runtime auth, then fake Google auth emits a Drive-ready event and Drive Sync reloads to no remote snapshot | authentication event is emitted | Account summary shows Drive-ready, and sync remains in Account detail rather than overview | C0+C1 |
| DT11 | Drive sync upload uses explicit local-latest confirmation | Account detail starts with no remote snapshot and fake sync repository returns an uploaded-local result from upload-local | user taps the compact sync icon, chooses `Upload local data to Drive`, and confirms `Upload to Drive` | Drive Sync returns to up-to-date status, legacy automatic `syncNow` is not called, and upload-local is called once | C0+C1 |
| DT12 | Web Drive authorization is requested from a user gesture | account detail starts with a stored Drive-ready account, fake Web auth emits a runtime account without Drive scope, and fake reconnect returns Drive appdata authorization | user taps `Reconnect Google Drive` | Account detail shows `Google Drive ready` and Drive Sync controller can load a no-remote status | C0+C1 |
| DT13 | Drive sync action fails with a project/API diagnostic | Account detail starts with no remote snapshot and fake sync repository returns a failed upload-local result containing a Drive API configuration message | user taps sync, chooses `Upload local data to Drive`, and confirms `Upload to Drive` | one generic failure status is visible, the technical diagnostic is visible as helper text, and compact sync retry remains available | C0+C1 |
| DT14 | Drive sync restore uses explicit Drive-latest confirmation | Account detail starts with a remote snapshot and fake sync repository returns a restored-remote result from restore-Drive | user taps sync, chooses `Download Drive data to this device`, and confirms `Restore from Drive` | Drive Sync returns to up-to-date status and restore-Drive is called once | C0+C1 |
| DT15 | Drive sync confirmation is canceled | Account detail starts with no remote snapshot and fake sync repository would upload if called | user taps sync, chooses `Upload local data to Drive`, then cancels confirmation | upload-local is not called and no upload success message is visible | C0+C1 |
