const matchResolveDelay = Duration(
  milliseconds: 450,
); // guard:raw-size-reviewed match feedback
const matchSuccessHoldDuration = Duration(
  milliseconds: 350,
); // guard:raw-size-reviewed match success hold
const matchFadeDuration = Duration(
  milliseconds: 300,
); // guard:raw-size-reviewed match fade
